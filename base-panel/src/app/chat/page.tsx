import { ChatView } from '@/components/chat/ChatView'

/** A blank conversation. The moment a turn creates one, the URL becomes /chat/<uuid>. */
export default function ChatPage() {
  return <ChatView />
}
